# Backend Configuration

# Server Port
PORT=8080

# Server Host (0.0.0.0 = all interfaces, localhost = local only)
HOST=0.0.0.0

# CORS Configuration
# For local development: All localhost/127.0.0.1 origins (any port) are automatically allowed
# For production: Add your production frontend URLs here (comma-separated)
# Example: ALLOWED_ORIGINS=https://cheesse.com,https://www.cheesse.com
ALLOWED_ORIGINS=
